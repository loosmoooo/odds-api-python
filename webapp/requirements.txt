# Web application dependencies (in addition to the odds-api-io SDK,
# installed from the repository root with `pip install -e .`)
flask>=3.0.0
flask-socketio>=5.3.0
python-socketio>=5.16.2
simple-websocket>=1.0.0
# Provides the `websocket` module, used to bridge the upstream
# Odds-API.io real-time feed to browser Socket.IO clients (see app.py).
websocket-client>=1.6.0
